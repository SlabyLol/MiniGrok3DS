#pragma once

namespace MiniGrokChat
{
    // Opens the chat (currently a stub)
    void Open();

    // Later: real API communication
    // bool SendMessage(const std::string &text, std::string &response);
    // bool SendScreen(const u8 *framebuffer, std::string &response);
}
