#pragma once

namespace FaceAnimation
{
    // Shows the current face (Idle / Talking / Thinking ...)
    void ShowIdle();
    void ShowThinking();
    void ShowTalking();
    void ShowHappy();

    // Later: real sprite animation with multiple frames
    // void Update(float delta);
    // void Draw(int x, int y);
}
